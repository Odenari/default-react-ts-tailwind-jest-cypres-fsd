import { AddressForm, NameForm } from '../features/user-registration';

const Registration = () => {
  return (
    <section>
      <NameForm />
      <AddressForm />
    </section>
  );
};

export default Registration;
